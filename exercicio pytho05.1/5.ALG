Algoritmo "5"
//
Var
// Seção de Declarações das variáveis 
ta,con:inteiro

Inicio
// Seção de Comandos, procedimento, funções, operadores, etc... 
escreval("escreva um numero pra tabuada")
leia(ta)
con <- 1
enquanto ( con <= 10) faca
         escreval(ta," X",con,":",(con * ta))
         con <- con + 1
fimenquanto

Fimalgoritmo