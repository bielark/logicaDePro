Algoritmo "7"

Var
// Seção de Declarações das variáveis 
sju,par,pre, t:real

Inicio
// Seção de Comandos, procedimento, funções, operadores, etc... 
escreval("valor do produto")
leia(pre)
sju<- sju / 10
escreval("valor em 10 parcela ", sju," deseja troca as parcelas")
leia(par)
t<- pre / par
escreva ("fica em ",t,"com a parcela",par)


Fimalgoritmo